--setTime(0)

--playerUUID = getPlayer()

--pos = getEntityPosition(playerUUID)

x = selfX
y = selfY


angle = math.rad(math.random()*360)
xPos = math.cos(angle)
yPos = math.sin(angle)

speed = math.random() - 0.5;

createParticle(0, x + 0.5, y + 0.5, xPos * speed, yPos * speed, 0.3)
