#pragma once

#include "Entity.h"


class InteractEntity : public Entity {
    public:
        InteractEntity();
        InteractEntity(glm::vec2 position, Categories::Entity_Type type, unsigned int id);

    private:

};
