#include "Item.h"

#include "World.h"
#include "Tile.h"

void Item::onLeftClick(Tile* selectedBlock, World* world) { /// TODO: Re-enable item stuff (this function)
    //Tile* b = createBlock((unsigned int)Categories::BlockIDs::AIR, selectedBlock->getPosition(), selectedBlock->getLayer());
    //b->setAmbientLight(selectedBlock->getLight());
    //world->setTile(b);
}
